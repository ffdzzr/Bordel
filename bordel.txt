alias ll="ls -lahtr --color=always | awk '{print \$1, \$3, \$4, \$5, \$9}' | column -t -c 1"

# vim.rc

autocmd VimEnter * if argc() == 0 | Explore | endif

" Open netrw like a sidebar
let g:netrw_banner = 0             " Remove banner
let g:netrw_liststyle = 3          " Tree-style view
let g:netrw_browse_split = 0       " Open files in current window
let g:netrw_altv = 1               " Open splits to the right
let g:netrw_winsize = 25           " Width of the netrw window
let g:netrw_keepdir = 0            " Don't mess with working directory

